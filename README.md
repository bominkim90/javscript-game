# js-game 레포지토리 정의
```
자바스크립트 game을 만들며 연습해보자! <기본 + ES6 문법> <생각하며 코드 치기>
위 목표를 두고 있고,
처음엔 순수 자바스크립트로,
점진적으로 프레임워크, ts 사용을 해볼 예정이다.

이 프로젝트? 레포지토리? 의 가장 큰 목표는
=> 생각하며 코드치기
```


## 1단계 : 숫자 야구 게임 구현
```
게임 구현을 설명서 그대로 구현하려함
기능단위로 쪼개는거부터 시작한게 아니라
일단 큰 기능 단위로 함수로 묶었음
그리고 전역변수로 예를들어 nowPlaying이라는 전역변수 flag값을 둬서
콘솔로 들어오는 입력값에 대한 처리방식을 바꾸려함 (이처럼 콘솔 입력값에 대한 처리방식이 바뀌는걸 phase라 칭함)
문제점은 그럼 phase가 바뀔때마다 그것에 대한 flag값을 만들어줘야하므로, 전역변수가 계속 늘어날 가능성이 있음
그래서 2단계에서 수정이 들어감
```


## 2단계 : 게임 기록 저장 및 조회 기능 추가
```
계속해서 늘어나는 기능들로 인해, 함수를 더욱 세분화 함
phase가 계속 늘어남에 따라 flag값을 전역변수로 더 선언하는 방식말고 다른 방식을 찾기로 함
프로그램 시작 시 등록된 이벤트 리스너 => inputInterface.on("line", first리스너)
기존 리스너 삭제(초기화) => inputInterface.removeAllListeners("line")
새 등록 => inputInterface.on("line", new리스너)
위와 같이 "line"이벤트에 대한 리스너를 초기화 하고 
각 phase에 대응하는 각 리스너를 등록함
```


## 가장 크게 느낀점
```
혼자 했다면, 내가 알고있던 지식으로만 해결하려 했겠지만
페어 프로그래밍을 진행하여, 다른 사람의 생각을 
=> 코드도 보고, 말로 들어면서 설명을 들으니까 엄청 이해가 잘 됨
=> 그리고 무엇보다 '정말 다른 방식으로도 코드를 짤 수 있구나' 라는걸 느낌

이런 말이 기억남
*** 
  교수나 강사가 가르쳐주는건 몇년 혹은 몇달이 지나면 실무에선 다른 버전으로 다시 공부해야 할 수 있다
  그러므로 어떤 기술을 어떻게 명확히 쓰느냐를 외우지 말고
  주어진 문제에 대한 답? 코드를 인터넷이나 책으로 공부를 했을때
  그것에 더 개선방향이 없을까? 나에게 맞게 고칠수는 없을까? 
  항상 그 생각을 가지는게 중요하다 말했다.
  그리고 그런말을 한 사람은 아직도 매일 그렇게 공부를 하고 있단다.. 
***
```